module.exports = {
    name: "report",
    category: "moderation",
    description: "Lets user know to contact staff LOL",
    run: async (bot, message, args) => {

        if(args.length < 1)
        message.reply('pussy boy')
        
    }
}